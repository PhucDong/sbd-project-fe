import { Box } from "@mui/material";
import MainHeadingLayout from "../layouts/MainHeadingLayout";

function ParameterSettings() {
  return (
    <Box>
      <MainHeadingLayout>Parameter Settings</MainHeadingLayout>
    </Box>
  )
}

export default ParameterSettings
